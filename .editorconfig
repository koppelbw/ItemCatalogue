root = true

[*.cs]
# CA2016: Forward the 'CancellationToken' parameter to methods that take one.
# Elevated to an error so any async method that has a token in scope but fails
# to thread it down fails the build, keeping cancellation wired end-to-end.
dotnet_diagnostic.CA2016.severity = error
